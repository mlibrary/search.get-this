services:
  web:
    build:
      context: .
      target: development
    ports:
      - "4567:4567"
      - "9394:9394"
    volumes:
      - .:/app
    env_file:
      - .env
      - env.development
    command:
      - bundle
      - exec
      - rackup
      - -p
      - "4567"
      - --host
      - 0.0.0.0
  css:
    build:
      context: .
      target: assets
    volumes:
      - .:/app
    command: npm run watch:css
  js:
    build:
      context: .
      target: assets
    volumes:
      - .:/app
    command: npm run watch:js
